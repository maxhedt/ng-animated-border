import { AnimatedBorderDirective } from './animated-border.directive';

describe('AnimatedBorderDirective', () => {
  it('should create an instance', () => {
    const directive = new AnimatedBorderDirective();
    expect(directive).toBeTruthy();
  });
});
